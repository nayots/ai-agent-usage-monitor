<UserControl x:Class="AiUsageMonitor.App.Views.Settings.WindowPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel>
    <!-- No "Always on top" here. Pinning is session state and belongs to the title bar's pin
         alone; this window is for how the widget works from now on. The line below is what tells
         anyone who reaches for it where it went, and what it now also does. -->
    <TextBlock Text="Pinning is on the widget's title bar. A pinned widget stays on top and stays open when you click another window, until you close the app." TextWrapping="Wrap" Margin="0,0,0,6" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" />
    <CheckBox Style="{StaticResource SettingsCheckBoxStyle}" Content="Show providers that are not installed" IsChecked="{Binding ShowUnavailableProviders, Mode=TwoWay}" AutomationProperties.Name="Show providers that are not installed" />
    <CheckBox x:Name="StartWithWindowsBox" Style="{StaticResource SettingsCheckBoxStyle}" Content="Start with Windows" IsChecked="{Binding StartWithWindows, Mode=TwoWay}" IsEnabled="{Binding CanStartWithWindows}" AutomationProperties.Name="Start with Windows" />
    <!-- Style is set only as a property element here. Setting the Style attribute as well and
         then nesting TextBlock.Style is MC3024 - the property can be set once. -->
    <TextBlock Text="{Binding StartWithWindowsUnavailableReason}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextTertiaryBrush}">
      <TextBlock.Style>
        <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
          <Setter Property="Visibility" Value="Visible" />
          <Style.Triggers><DataTrigger Binding="{Binding CanStartWithWindows}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers>
        </Style>
      </TextBlock.Style>
    </TextBlock>
    <CheckBox x:Name="GlobalHotkeyBox" Style="{StaticResource SettingsCheckBoxStyle}" Content="{Binding GlobalHotkeyLabel}" IsChecked="{Binding GlobalHotkeyEnabled, Mode=TwoWay}" AutomationProperties.Name="Ctrl+Alt+Q" />
    <TextBlock Text="{Binding GlobalHotkeyUnavailableReason}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextTertiaryBrush}">
      <TextBlock.Style>
        <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
          <Setter Property="Visibility" Value="Collapsed" />
          <Style.Triggers><DataTrigger Binding="{Binding HasGlobalHotkeyWarning}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
        </Style>
      </TextBlock.Style>
    </TextBlock>
    <Button Style="{StaticResource SettingsActionButtonStyle}" Content="Reset window position" Command="{Binding ResetPositionCommand}" AutomationProperties.Name="Reset window position" />
  </StackPanel>
</UserControl>
