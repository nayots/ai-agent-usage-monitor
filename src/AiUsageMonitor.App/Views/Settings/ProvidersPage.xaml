<UserControl x:Class="AiUsageMonitor.App.Views.Settings.ProvidersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel>
    <TextBlock Text="{Binding ProviderPreferencesHintText}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" />
    <ItemsControl ItemsSource="{Binding ProviderPreferences}" Focusable="False">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border BorderBrush="{DynamicResource WidgetCardStrokeBrush}" BorderThickness="0,0,0,1">
            <Border.Style>
              <Style TargetType="Border">
                <Setter Property="Margin" Value="0,8,0,0" />
                <Setter Property="Padding" Value="0,0,0,8" />
              </Style>
            </Border.Style>
            <StackPanel>
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Style="{StaticResource BodySmallTextStyle}" Foreground="{DynamicResource TextPrimaryBrush}" />
                <CheckBox Grid.Column="1" Style="{StaticResource SettingsCheckBoxStyle}" Content="Show" IsChecked="{Binding IsVisible, Mode=TwoWay}" AutomationProperties.Name="{Binding DisplayName, StringFormat=Show {0}}" />
                <!-- The two reorder buttons are link-styled, so nothing but this margin separates
                     them: without it they render as one run of text reading "Move upMove down". -->
                <Button Grid.Column="2" Margin="16,0,0,0" Style="{StaticResource SettingsActionButtonStyle}" Content="Move up" Command="{Binding MoveUpCommand}" IsEnabled="{Binding CanMoveUp}" AutomationProperties.Name="{Binding DisplayName, StringFormat=Move {0} up}" />
                <Button Grid.Column="3" Margin="16,0,0,0" Style="{StaticResource SettingsActionButtonStyle}" Content="Move down" Command="{Binding MoveDownCommand}" IsEnabled="{Binding CanMoveDown}" AutomationProperties.Name="{Binding DisplayName, StringFormat=Move {0} down}" />
              </Grid>
              <TextBlock Text="Check this provider every" Foreground="{DynamicResource TextTertiaryBrush}">
                <TextBlock.Style>
                  <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
                    <Setter Property="Margin" Value="0,4,0,3" />
                  </Style>
                </TextBlock.Style>
              </TextBlock>
              <ItemsControl ItemsSource="{Binding Intervals}" Focusable="False">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <RadioButton Content="{Binding Label}" GroupName="{Binding GroupName}" IsChecked="{Binding IsSelected, Mode=TwoWay}">
                      <AutomationProperties.Name>
                        <MultiBinding StringFormat="{}{0} {1} interval">
                          <Binding Path="DataContext.DisplayName" RelativeSource="{RelativeSource AncestorType=ItemsControl, AncestorLevel=2}" />
                          <Binding Path="Label" />
                        </MultiBinding>
                      </AutomationProperties.Name>
                      <RadioButton.Style>
                        <Style TargetType="RadioButton" BasedOn="{StaticResource SettingsRadioButtonStyle}">
                          <Setter Property="Margin" Value="0,0,4,4" />
                        </Style>
                      </RadioButton.Style>
                    </RadioButton>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <Button Style="{StaticResource SettingsActionButtonStyle}" Content="Re-check providers" Command="{Binding RecheckProvidersCommand}" AutomationProperties.Name="Re-check providers" />
  </StackPanel>
</UserControl>
