<Window x:Class="AiUsageMonitor.App.Views.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quota Monitor diagnostics"
        Width="520" SizeToContent="Height"
        WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WidgetWindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontFamily="{StaticResource WidgetFontFamily}"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Margin="14,10,14,14">
      <ItemsControl ItemsSource="{Binding Sections}" Focusable="False">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <StackPanel Margin="0,0,0,12">
              <TextBlock Text="{Binding Title}" Style="{StaticResource SettingsSectionTextStyle}" />
              <TextBlock Text="{Binding Subtitle}" TextWrapping="Wrap" Foreground="{DynamicResource TextTertiaryBrush}">
                <TextBlock.Style>
                  <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding Subtitle}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style>
                </TextBlock.Style>
              </TextBlock>
              <ItemsControl ItemsSource="{Binding Fields}" Margin="0,3,0,2" Focusable="False">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Grid Margin="0,1">
                      <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                      <TextBlock Text="{Binding Label}" Foreground="{DynamicResource TextTertiaryBrush}" Style="{StaticResource CaptionTextStyle}" />
                      <TextBlock Grid.Column="1" Text="{Binding Value}" TextWrapping="Wrap" IsHitTestVisible="True" Foreground="{DynamicResource TextPrimaryBrush}" Style="{StaticResource CaptionTextStyle}" />
                    </Grid>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
              <ItemsControl ItemsSource="{Binding Lines}" Focusable="False">
                <ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" /></DataTemplate></ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      <TextBlock Text="{Binding CopyHintText}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4" />
      <StackPanel Orientation="Horizontal">
        <Button Style="{StaticResource SettingsActionButtonStyle}" Content="Copy diagnostics" Command="{Binding CopyCommand}" AutomationProperties.Name="Copy diagnostics" />
        <Button Style="{StaticResource SettingsActionButtonStyle}" Content="Open logs folder" Command="{Binding OpenLogsCommand}" AutomationProperties.Name="Open logs folder" />
      </StackPanel>
      <TextBlock x:Name="CopyConfirmation" Text="{Binding CopyConfirmationText}" TextWrapping="Wrap" Foreground="{DynamicResource TextTertiaryBrush}">
        <TextBlock.Style><Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding CopyConfirmationText}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
      </TextBlock>
    </StackPanel>
  </ScrollViewer>
</Window>
