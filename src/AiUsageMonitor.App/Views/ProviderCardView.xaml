<UserControl x:Class="AiUsageMonitor.App.Views.ProviderCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:AiUsageMonitor.App.Controls"
             xmlns:views="clr-namespace:AiUsageMonitor.App.Views">
  <Border Background="{DynamicResource WidgetLayerBackgroundBrush}" BorderBrush="{DynamicResource WidgetCardStrokeBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusCard}" Padding="{DynamicResource ProviderCardPadding}">
    <StackPanel>
      <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <Border Grid.Column="0" Width="16" Height="16" CornerRadius="{DynamicResource RadiusChip}" Background="{DynamicResource WidgetTokenChipBackgroundBrush}" BorderBrush="{DynamicResource WidgetTokenChipStrokeBrush}" BorderThickness="1" VerticalAlignment="Center"><TextBlock Text="{Binding Monogram}" FontSize="8.5" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" /></Border>
        <TextBlock Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center" Style="{StaticResource SubtitleTextStyle}" Text="{Binding DisplayName}" Foreground="{DynamicResource TextPrimaryBrush}" />
        <TextBlock Grid.Column="2" Margin="7,0,0,0" VerticalAlignment="Center" Style="{StaticResource CaptionTextStyle}" Text="{Binding VersionText}" Foreground="{DynamicResource TextTertiaryBrush}" />
        <controls:TierBadge Grid.Column="4" VerticalAlignment="Center" Tier="{Binding Tier}" />
      </Grid>
      <StackPanel Orientation="Horizontal" Margin="0,4,0,7"><controls:StateChip State="{Binding State}" Label="{Binding StateLabel}" VerticalAlignment="Center" /><TextBlock x:Name="TimestampLine" Margin="6,0,0,0" VerticalAlignment="Center" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" Visibility="{Binding HasTimestampText, Converter={StaticResource BooleanToVisibility}}"><Run Text="&#x00B7;" /><Run Text=" " /><Run Text="{Binding TimestampText, Mode=OneWay}" /></TextBlock></StackPanel>
      <Border Margin="0,0,0,6" Padding="7,5" CornerRadius="{DynamicResource RadiusControl}" Background="{DynamicResource WidgetLayerAltBackgroundBrush}" BorderBrush="{DynamicResource WidgetCardStrokeBrush}" BorderThickness="1" Visibility="{Binding IsStale, Converter={StaticResource BooleanToVisibility}}">
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <Rectangle Grid.Column="0" Width="7" Height="7" Margin="0,3,6,0" VerticalAlignment="Top" Fill="{DynamicResource StateWarnBrush}" RenderTransformOrigin="0.5,0.5"><Rectangle.RenderTransform><RotateTransform Angle="45" /></Rectangle.RenderTransform></Rectangle>
          <!-- The age is not repeated here. It is four pixels above, in the header's "Updated ..."
               line, computed from the same timestamp under the same condition. -->
          <TextBlock Grid.Column="1" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextSecondaryBrush}" Text="Values may no longer be current." />
          <Button Grid.Column="2" Margin="6,0,0,0" VerticalAlignment="Top" Content="Refresh" Style="{StaticResource LinkButtonStyle}" Command="{Binding RetryCommand}" />
        </Grid>
      </Border>
      <Grid Margin="0,0,0,3" Visibility="{Binding HasWindows, Converter={StaticResource BooleanToVisibility}}"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="8" /><ColumnDefinition Width="42" /><ColumnDefinition Width="8" /><ColumnDefinition Width="62" /></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="WINDOW" Style="{StaticResource CaptionMicroTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" /><TextBlock Grid.Column="2" Text="USED" TextAlignment="Right" Style="{StaticResource CaptionMicroTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" /><TextBlock Grid.Column="4" Text="RESETS IN" TextAlignment="Right" Style="{StaticResource CaptionMicroTextStyle}" Foreground="{DynamicResource TextTertiaryBrush}" /></Grid>
      <ItemsControl ItemsSource="{Binding Windows}" Focusable="False"><ItemsControl.ItemTemplate><DataTemplate><views:QuotaRowView /></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
      <Grid Margin="0,8,0,0" Visibility="{Binding HasNotice, Converter={StaticResource BooleanToVisibility}}"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
        <Polygon Grid.Column="0" Points="0,9 5,0 10,9" Margin="0,3,7,0" VerticalAlignment="Top" Fill="{DynamicResource StateBadBrush}" Visibility="{Binding Notice.IsAlert, Converter={StaticResource BooleanToVisibility}}" />
        <StackPanel Grid.Column="1">
          <TextBlock Text="{Binding Notice.Title}" TextWrapping="Wrap" FontSize="12" FontWeight="SemiBold" FontFamily="{StaticResource WidgetFontFamily}"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" /><Style.Triggers><DataTrigger Binding="{Binding Notice.IsAlert}" Value="True"><Setter Property="Foreground" Value="{DynamicResource StateBadBrush}" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
          <TextBlock Text="{Binding Notice.Body}" TextWrapping="Wrap" Margin="0,2,0,0" Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource TextSecondaryBrush}" />
          <Button Margin="0,7,0,0" HorizontalAlignment="Left" Padding="9,3" Content="{Binding Notice.ActionText}" Command="{Binding RetryCommand}"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource LinkButtonStyle}"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding Notice.ActionText}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style></Button.Style></Button>
        </StackPanel>
      </Grid>
    </StackPanel>
  </Border>
</UserControl>
